---
---

<header class="header">
  <div class="logo">
    <div class="logo-icon">♟</div>
    <span class="logo-text">ReperTREE</span>
    <span class="logo-tag">AI</span>
  </div>
  <nav class="header-nav">
    <a href="/" class="nav-link active">Tablero</a>
    <a href="/repertorio" class="nav-link">Repertorio</a>
    <a href="/analisis" class="nav-link">Análisis IA</a>
  </nav>
  <div class="header-actions">
    <button class="hb" id="btnFlip">⇄ Voltear</button>
    <button class="hb" id="btnNew">↺ Nueva</button>
    <button class="hb" id="btnFen">FEN / PGN</button>
    <button class="hb accent" id="btnAnalyze">✦ Analizar IA</button>
  </div>
</header>

<style>
  .header {
    background: var(--bg-1);
    border-bottom: 1px solid var(--border);
    padding: 0 20px;
    display: flex;
    align-items: center;
    gap: 16px;
    height: 52px;
    flex-shrink: 0;
    position: relative;
    z-index: 10;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 16px;
    font-weight: 800;
    letter-spacing: -0.02em;
    white-space: nowrap;
  }

  .logo-icon {
    width: 28px;
    height: 28px;
    border-radius: 6px;
    background: linear-gradient(135deg, var(--accent), #d4860a);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 15px;
    line-height: 1;
    box-shadow: 0 2px 8px rgba(232, 168, 56, 0.3);
  }

  .logo-text {
    background: linear-gradient(135deg, var(--accent), #f0c866);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .logo-tag {
    font-size: 9px;
    font-weight: 600;
    color: var(--bg-0);
    background: var(--accent);
    padding: 1px 5px;
    border-radius: 4px;
    letter-spacing: 0.04em;
    margin-left: -4px;
    line-height: 1.4;
  }

  .header-nav {
    display: flex;
    gap: 4px;
    margin-left: 24px;
  }

  .nav-link {
    padding: 6px 12px;
    border-radius: var(--radius-sm);
    font-size: 13px;
    font-weight: 500;
    color: var(--text-2);
    text-decoration: none;
    transition: all var(--transition);
  }

  .nav-link:hover {
    color: var(--text-0);
    background: var(--bg-3);
    text-decoration: none;
  }

  .nav-link.active {
    color: var(--accent);
    background: var(--accent-dim);
  }

  .header-actions {
    display: flex;
    gap: 6px;
    margin-left: auto;
  }

  .hb {
    padding: 6px 12px;
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    background: var(--bg-3);
    color: var(--text-1);
    cursor: pointer;
    font-size: 12px;
    font-weight: 500;
    font-family: var(--font-body);
    transition: all var(--transition);
    display: flex;
    align-items: center;
    gap: 5px;
  }

  .hb:hover {
    background: var(--bg-4);
    border-color: var(--border-h);
    color: var(--text-0);
  }

  .hb.accent {
    background: linear-gradient(135deg, var(--accent), #d4920a);
    color: var(--bg-0);
    border-color: transparent;
    font-weight: 700;
    box-shadow: 0 2px 10px rgba(232, 168, 56, 0.25);
  }

  .hb.accent:hover {
    box-shadow: 0 2px 14px rgba(232, 168, 56, 0.4);
    transform: translateY(-1px);
  }

  @media (max-width: 900px) {
    .header-nav { display: none; }
  }
</style>
